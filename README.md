# dotnet-mcp
A .NET library that automatically exposes your application's APIs as an MCP (Model Context Protocol) server, enabling AI assistants and agents to discover, understand, and securely invoke your endpoints with minimal configuration.
